doctype html
html(lang="vi")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Quên mật khẩu
    link(rel="stylesheet", href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css")

  body
    .container.mt-5
      .row.justify-content-center
        .col-md-5
          h2.text-center.mb-4.text-warning Quên mật khẩu

          if message
            .alert.alert-success.text-center #{message}

          if error
            .alert.alert-danger.text-center #{error}

          form(method="POST", action="/user/forgot-password")
            .mb-3
              label.form-label(for="email") Nhập email đã đăng ký
              input.form-control(type="email", name="email", id="email", required)

            button.btn.btn-warning.w-100(type="submit") Gửi liên kết đặt lại mật khẩu

          .text-center.mt-3
            a(href="/user/login") ← Quay lại đăng nhập

    script(src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js")
